# Fig configuration file for Hypermasher

django:
  build: .
  ports:
    - "8000:8000"
  links:
    - db:postgres
  environment:
    TWITTER_API_KEY:
    TWITTER_API_SECRET:
    DJANGO_SETTINGS_MODULE:
  volumes:
    - .:/usr/src/app

db:
  image: postgres
